<Page
    x:Class="Zabawa_z_instrukcjami_if_else.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Zabawa_z_instrukcjami_if_else"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}" Width="700" Height="300">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Button x:Name="changeText" Content="Kliknięcie zmienia etykietę"
            HorizontalAlignment="Center" Click="changeText_Click"/>

        <CheckBox x:Name="enableCheckbox" Content="Włącza zmianę etykiety"
              HorizontalAlignment="Center" IsChecked="true" Grid.Column="1"/>

        <TextBlock x:Name="labelToChange" Grid.Row="1" TextWrapping="Wrap"
               Text="Naciśnij przycisk, aby zmienić tekst"
               HorizontalAlignment="Center" VerticalAlignment="Center"
               Grid.ColumnSpan="2" FontSize="28"/>
    </Grid>

</Page>
